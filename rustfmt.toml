# The tree is written to 100 columns, and puts a statement on one line wherever
# it fits rather than using rustfmt's narrower per-construct defaults.
max_width = 100
use_small_heuristics = "Max"
newline_style = "Unix"
